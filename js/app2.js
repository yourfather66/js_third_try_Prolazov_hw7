/*let endNum = 20;
let stringOfNum = "";
for (let i = 10; i <= endNum; i++) {
  if (i === endNum) {
    stringOfNum += `${Math.pow(i, 2)}`;
  } else {
    stringOfNum += `${Math.pow(i, 2)},`;
  }
}
console.log(stringOfNum);
